name: Deploy

on: push

jobs:
  deployment:
    environment: prod
    runs-on: ubuntu-latest
    steps:
    - name: Deploy Stage
      uses: fjogeleit/http-request-action@master
      with:
        url: ${{ env.DEPLOY_URL }}
        method: 'GET'
